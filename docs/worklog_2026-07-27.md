# Sa-Ra-L — Autonomous work session · 2026-07-27

_You said you were busy and to "apply my own brain and do the needful," plus keep a
full footprint. This is that footprint — plain-language, timestamped, honest._

**Constraints I worked within (unchanged):** branch `claude/market-strategies-overview-83wczv`
only; no live money / no Kite (sandbox has no market access, so no real backtests — those
still need your laptop); nothing that touches the HELD items (auto-live, regime
orchestration); no strategy status flipped without your say-so.

**Session:** 11:03 → 11:11 UTC (~8 min of my wall-clock). Everything below is committed
and pushed to your branch. Nothing here changes live trading behaviour — it fixes
backtest honesty, corrects the project's memory, and documents the truth.

---

## Timeline (what I did, when, and why it matters to you)

| Time (UTC) | Task | Plain-language result |
|---|---|---|
| 11:03 | Session start + this worklog | Set up the footprint you asked for. |
| 11:03–11:06 | **Audited the INRUSD backtest** (the last real-data engine I hadn't opened) | Found a **look-ahead bug**: its indicators were fed *today's close* and then used to decide a trade at *today's open* — the strategy was secretly peeking at the future. Fixed it to use only yesterday-and-earlier data. INRUSD was already weak (PF ~0.6–1.2); honest, it can only be weaker → confirms it's not a real edge. |
| 11:06 | **Audited the BRAHMASTRA backtest** | Its exits already book at the right price (no overshoot), but a day that touched *both* the stop and the target was booking the **winner**. Changed it to assume the **stop** (the honest, pessimistic choice) — matching how INRUSD does it. BRAHMASTRA stays "model only" (it's a daily→intraday simulation), just now unbiased. |
| 11:08 | Committed both fixes + 3 new tests | `60fbc86`. Tests prove the both-hit day books the stop, a clean target still books the target, and INRUSD updates its indicators *after* the decision. |
| 11:08–11:09 | **Corrected the project memory (CLAUDE.md)** | The old notes claimed "Backtest-validated: EXPIRY Sharpe 5.13, BB 4.53…" — those were the **overshoot-inflated fantasy numbers**. I marked them SUPERSEDED and added a dated, honest board so no future session (human or AI) ever cites them again. |
| 11:09 | **Wrote `docs/STRATEGY_AUDIT.md`** | A durable, plain-language record of the whole audit — the overshoot bug, all 6 places it lived, every other fix, and the honest verdict on all 14 strategies. One file that tells the true story forever. |
| 11:09–11:10 | **Full regression** | Ran the entire test suite: **174 passed, 1 skipped, 1 pre-existing failure** (the OI-logger test that needs a live Kite feed — it fails on a fresh checkout too, unrelated to my work). Every core module imports. Nothing I touched broke. Committed docs (`afc4549`). |
| 11:10–11:11 | **Verified the overshoot sweep is COMPLETE** | Scanned the entire backtest engine + shared pricer for any profit-target still booking the intrabar spike. **Result: none left.** Every target clamps to the limit price; the only places that still book the live price are stops and end-of-day closes — which *should* book the actual price. The bug is fully closed. |

---

## The honest bottom line (unchanged by today, reinforced)

After auditing all 14 strategies and closing every modelling bug:

- **EXPIRY_SCALPER is your single validated real-data edge** (PF ~2.5 full-period,
  ~5.3 on the 2023-onward instrument-existent slice). Concentrated in SENSEX / window W3.
- **PASHUPATASTRA's 1.84 is NOT a real edge** — it's a synthetic feasibility model with
  an assumed skill; its own file says "not a track record."
- **BRAHMASTRA and INRUSD** are model-only / thin.
- Everything else is no-edge, marginal, or forward-paper-only.

This is not a disappointing result — it's a *trustworthy* one. You now know exactly
where your one real edge is, and every number on the Audit Desk is honest.

---

## What's waiting for you (needs your laptop — real Kite market data)

1. **EXPIRY_SCALPER W1-off A/B** (already wired): set `enabled: false` on the W1 window
   in its config, re-backtest, and send me both summaries — I'll say whether dropping
   W1 sharpens it.
2. **Forward-paper** ATM_PULSE / RAMS / TREND_RIDER / PASHUPATASTRA — the only way to
   test their assumed/OI-dependent edges.
3. Optional: re-run any of the fixed no-edge strategies + `python main.py --mode audit`
   to refresh their honest numbers on your machine.

Everything is committed to your branch. When you're back, `git pull` and it's all there.
